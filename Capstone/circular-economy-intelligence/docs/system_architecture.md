# System Architecture

React frontend → FastAPI backend → ML inference/services → SQLite database and reference recycling rates.

Primary model: 12-class waste image classification. Optional YOLO detection can be added after bounding-box annotation data is available.
