version: "3.9"
services:
  backend:
    build:
      context: ..
      dockerfile: deployment/backend.Dockerfile
    ports: ["8000:8000"]
    env_file: ../.env
  frontend:
    build:
      context: ..
      dockerfile: deployment/frontend.Dockerfile
    ports: ["80:80"]
    depends_on: [backend]
